{
  "name": "recursive-map",
  "version": "0.1.1",
  "main": "dist/recursive-map.js",
  "module": "dist/recursive-map.es.js",
  "author": "Cyrus Venn Casada",
  "scripts": {
    "build": "microbundle --sourcemap=false --strict --compress=false",
    "dev": "microbundle watch",
    "docs": "documentation readme index.js -q --section API"
  },
  "license": "MIT",
  "devDependencies": {
    "documentation": "^5.4.0",
    "microbundle": "^0.4.4"
  },
  "dependencies": {
    "curry-n": "^1.0.2"
  }
}
